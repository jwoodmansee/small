<h2><%= @post.content %> Comment</h2>

<% if @post.any? %>
  <ul>
    <% @comments.each do |comment| %>
      <li class='comment-info-text'> <%=comment.info%> -
      <%= link_to 'Show', post_comment_path(@post, @comment) %></li>
      <%end%>
  </ul>
<%else%>
    <h3>No comments have been submitted. Click the link below to add a comment</h3>
<%end%>

<%= link_to 'Add Comment', new_post_comment_path(@post, @comment) %>    
